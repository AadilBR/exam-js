
let tab = [2, 3, 5]

const average = () => {
  return ((tab[0] + tab[1] + tab[2]) / 2)
}
console.log(average(tab))


l/* et sum = 0
for (let i = 0; i < tab.length; ++i) {

  return (sum += tab[i]) / 2
}


console.log(`sum = ${sum}`) */